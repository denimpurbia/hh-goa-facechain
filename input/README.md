# Input Directory

Place input face images here for processing with FaceChain Verify.

Supported image formats:
- JPEG / JPG (`.jpg`, `.jpeg`)
- PNG (`.png`)
- WebP (`.webp`)

### Usage Example:
```bash
python -m src.main --input input/my_face.jpg
```
